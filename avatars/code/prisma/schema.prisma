generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "windows"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model AvatarOnderdeel {
  id         Int      @id @default(autoincrement())
  naam       String
  categorie  String   
  waarde     Int      @default(0) 
  afbeelding String   
  hex_kleur  String?
}